apiVersion: v1
kind: Service
metadata:
  name: oracle-mcp
  labels:
    app: oracle-mcp
spec:
  type: NodePort
  selector:
    app: oracle-mcp
  ports:
    - name: http
      port: 8080
      targetPort: http
      nodePort: 30022
